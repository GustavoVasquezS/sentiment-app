# Variables usadas por docker-compose.yml a nivel de servicio "postgres".
# Cada app/servicio tiene ademas su propio .env.example (apps/api, apps/web, services/ml).
POSTGRES_DB=sentiment_dev
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
