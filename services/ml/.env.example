MODEL_PATH=sentiment_bundle_es_pt_v2.joblib
# Lista separada por comas de orígenes permitidos (en la arquitectura nueva
# normalmente solo el backend Node, no el navegador, llama a este servicio).
ALLOWED_ORIGINS=http://localhost:8080
PORT=8000
